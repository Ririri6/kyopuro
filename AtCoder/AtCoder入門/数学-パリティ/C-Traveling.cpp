#include <bits/stdc++.h>
using namespace std;
 
int main() {
  long long N;
  cin>>N;
  vector<long long> t(N);
  vector<long long> x(N);
  vector<long long> y(N);

  for(int i=0;i<N;i++){
    cin>>t[i]>>x[i]>>y[i];
  }

  
}
